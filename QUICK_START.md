# クイックスタートガイド

このプロジェクトを素早く起動するためのガイドです。

## 🚀 最速で起動する方法

### Windows ユーザー（最も簡単）

**方法 1: ダブルクリック**

プロジェクトルートの `run-web.bat` をダブルクリック

→ ブラウザで `http://localhost:8080` を開く

**方法 2: コマンドライン（推奨）**

```cmd
# バッチファイル
scripts\dev.bat web

# または PowerShell
.\scripts\dev.ps1 web
```

### Makefile を使用（全プラットフォーム）

```bash
# Web Server モードで起動
make web

# すべてのコマンドを表示
make help
```

## 📋 よく使うコマンド

### すべての起動方法

| 方法                  | コマンド                  | 説明                       |
| --------------------- | ------------------------- | -------------------------- |
| **バッチファイル**    | `scripts\dev.bat web`     | Windows ネイティブ         |
| **PowerShell**        | `.\scripts\dev.ps1 web`   | カラー出力、詳細情報       |
| **Makefile**          | `make web`                | クロスプラットフォーム     |
| **VS Code**           | `Ctrl+Shift+P` → タスク   | IDE 統合                   |
| **簡易バッチ**        | `run-web.bat`             | ダブルクリック可能         |
| **Flutter コマンド**  | `flutter run -d web-server --web-port=8080` | 元のコマンド |

## 🎯 コマンド一覧

### バッチファイル（scripts\dev.bat）

```cmd
scripts\dev.bat web       # Web Server モードで起動
scripts\dev.bat chrome    # Chrome で起動
scripts\dev.bat edge      # Edge で起動
scripts\dev.bat clean     # ビルドキャッシュをクリア
scripts\dev.bat build     # 本番用にビルド
scripts\dev.bat install   # 依存関係をインストール
scripts\dev.bat generate  # Riverpod のコード生成
scripts\dev.bat watch     # Riverpod のコード生成を監視
```

### PowerShell（scripts\dev.ps1）

```powershell
.\scripts\dev.ps1 web       # Web Server モードで起動
.\scripts\dev.ps1 chrome    # Chrome で起動
.\scripts\dev.ps1 edge      # Edge で起動
.\scripts\dev.ps1 clean     # ビルドキャッシュをクリア
.\scripts\dev.ps1 build     # 本番用にビルド
.\scripts\dev.ps1 install   # 依存関係をインストール
.\scripts\dev.ps1 generate  # Riverpod のコード生成
.\scripts\dev.ps1 watch     # Riverpod のコード生成を監視
```

### Makefile

```bash
make web          # Web Server モードで起動
make web-chrome   # Chrome で起動
make web-edge     # Edge で起動
make clean        # ビルドキャッシュをクリア
make build-web    # 本番用にビルド
make install      # 依存関係をインストール
make generate     # Riverpod のコード生成
make watch        # Riverpod のコード生成を監視
make test         # テストを実行
make format       # コードをフォーマット
make analyze      # コードを分析
```

## 🔧 初回セットアップ

### 1. 依存関係をインストール

```bash
# どれか一つを実行
make install
scripts\dev.bat install
.\scripts\dev.ps1 install
flutter pub get
```

### 2. Riverpod のコード生成（必要な場合）

```bash
# どれか一つを実行
make generate
scripts\dev.bat generate
.\scripts\dev.ps1 generate
```

### 3. アプリケーションを起動

```bash
# どれか一つを実行
make web
scripts\dev.bat web
.\scripts\dev.ps1 web
run-web.bat  # またはダブルクリック
```

### 4. ブラウザで開く

```
http://localhost:8080
```

## 💡 開発のヒント

### ホットリロード

アプリケーション実行中にターミナルで：

- **`r`** - ホットリロード（変更を素早く適用）
- **`R`** - ホットリスタート（完全に再起動）
- **`q`** - 終了

### VS Code でのタスク実行

1. `Ctrl + Shift + P` を押す
2. "Tasks: Run Task" を入力
3. 実行したいタスクを選択

### スタイルのデバッグ

1. ブラウザで `F12` を押して開発者ツールを開く
2. Elements タブで要素を検証
3. HTML レンダラーが設定済みなので、スタイルが確認可能

### コード生成の自動化

Riverpod のコードを編集する際、自動生成を有効化：

```bash
make watch
# または
scripts\dev.bat watch
```

ファイルを保存すると自動的にコードが生成されます。

## 🐛 トラブルシューティング

### ポートが使用中

```bash
# 別のポートを使用
flutter run -d web-server --web-port=3000
```

### Chrome が起動しない

→ Web Server モードを使用（推奨）:

```bash
make web
```

### ビルドエラー

```bash
# クリーンビルド
make clean
make web
```

### PowerShell スクリプトが実行できない

```powershell
# 実行ポリシーを変更
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

## 📚 詳細なドキュメント

より詳しい情報は以下のドキュメントを参照してください：

- **[開発用コマンド一覧](document/開発用コマンド一覧.md)** - すべてのコマンドの詳細
- **[Flutter Web起動問題の解決方法](document/Flutter%20Web起動問題の解決方法.md)** - トラブルシューティング
- **[Flutter Web様式検査問題の解決](document/Flutter%20Web様式検査問題の解決.md)** - スタイル検証の設定

## 🎨 推奨される開発ワークフロー

### 日常の開発

1. **起動**
   ```bash
   make web
   ```

2. **ブラウザで開く**
   ```
   http://localhost:8080
   ```

3. **コード編集**
   - ファイルを編集して保存

4. **ホットリロード**
   - ターミナルで `r` を押す

5. **デバッグ**
   - ブラウザで `F12` → Elements タブ

### Riverpod を使用する場合

別のターミナルでコード生成を監視：

```bash
make watch
```

### 本番ビルド前

```bash
# 1. テストを実行
make test

# 2. コード分析
make analyze

# 3. フォーマット
make format

# 4. ビルド
make build-web
```

## 🚢 本番環境へのデプロイ

### ビルド

```bash
make build-web
```

生成されたファイル: `build/web/`

### デプロイ先の例

- Firebase Hosting
- Netlify
- Vercel
- GitHub Pages
- Azure Static Web Apps

詳細は各プラットフォームのドキュメントを参照してください。

---

**質問やお問い合わせは Issues までお願いします！**

